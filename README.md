# Naive, Rabin-Karp, and KMP Algorithms for String Matching

This project implements and compares three classical string matching algorithms:
- Naive String Matching
- Knuth-Morris-Pratt (KMP)
- Rabin-Karp

The program finds pattern occurrences in a text and compares the number of character comparisons performed by each algorithm.
